# Docker-NGINX

## Overview
This project runs a simple static website using Docker and NGINX.

## Tech Stack
- Docker
- NGINX

## Steps to Run
1. docker build -t my-first-nginx-app .
2. docker run -d -p 8080:80 my-first-nginx-app
3. Open http://localhost:8080
